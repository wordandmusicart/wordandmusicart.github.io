# word&music

Temporary one-screen website for word&music, published with GitHub Pages.

## Verify locally

```sh
npm test
npm run serve
```

`npm test` also builds the exact public files in `dist/`. Then open <http://localhost:4173>.
